apiVersion: apps/v1
kind: Deployment
metadata:
  name: migration-tracker
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: APP_ENVIRONMENT
              value: staging
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi

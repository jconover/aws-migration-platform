apiVersion: v1
kind: ServiceAccount
metadata:
  name: migration-tracker
  annotations:
    # Overlays patch in the IRSA role ARN emitted by terraform output app_role_arn.
    eks.amazonaws.com/role-arn: PLACEHOLDER
automountServiceAccountToken: true

apiVersion: v1
kind: Service
metadata:
  name: migration-tracker
  labels:
    app.kubernetes.io/name: migration-tracker
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: migration-tracker
